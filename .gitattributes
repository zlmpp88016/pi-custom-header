# Normalize line endings: use LF in the repo for all text files.
* text=auto eol=lf

# Header templates mirror raw HTTP captures — keep LF.
*.headers text eol=lf

# Lockfiles / binaries
bun.lock -diff
